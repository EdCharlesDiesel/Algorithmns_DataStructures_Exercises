using Xunit;

namespace LinearSearch.Tests;

public class LinearSearchUnitTest
{
    [Fact(Skip ="Read the documentation")]

    public void Test1()
    {

       // LinearSearchClass linearSearch = new LinearSearchClass();
        int[] A = { 84, 21, 47, 96, 15 };
      //  int found = linearSearch.linearsearch(A, 5, 15);
        //Assert.Equals(found, t.EqualTo(1));
        //Assert.AreEqual(4, found);

    }
}